
#Develop
New-Item -ItemType SymbolicLink -Path "./index.html" -Target "./dist/index.html"

##Front
deno task lume

##Back
deno task start

#Production
Replace into main.ts
```
const ROOT_DIR = "./dist/index.html", ROOT_DIR_PATH = "/";
==>
const ROOT_DIR = "./index.html", ROOT_DIR_PATH = "/";
```

##Front
deno task lume

##Back
deno compile --no-check --output ./dist/index ./src/main.ts

